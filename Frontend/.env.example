# Copy to .env or merge into your mode-specific env. Vite exposes only VITE_* vars.
VITE_APP_API_URL=http://127.0.0.1:8000
# Omit or set to anything other than "false" to use Riva-backed /speech/* routes.
VITE_RIVA_SPEECH=true
